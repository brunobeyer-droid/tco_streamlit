# Backup Workspace

Use this folder to store local backup artifacts that you want to track by commit.

Suggested structure:
- `backups/user_local/raw/` for original backup files
- `backups/user_local/notes/` for restore notes and decisions

Commit in small steps to keep a clear recovery timeline.
