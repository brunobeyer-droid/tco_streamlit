# Risk Patterns

Use these default pattern families in the checker report:

- ADO feature dump scope:
  - `ado`
  - `azure devops`
  - `feature`

- Apptio NWF/category scope:
  - `nwf`
  - `apptio`
  - `category`

Tune only when false positives are clearly documented in the report.
